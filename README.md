# Propulsion Prediction
- Before cloning the repo, install git large file system ([LFS](https://git-lfs.com/))
- Use the command:
```
git lfs install
```
- Then Clone the repo
```
git clone https://github.com/PropelSense/propulsion_prediction.git
```

**The Readme file will be updated as the work progresses.**
